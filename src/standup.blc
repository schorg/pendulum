import params "params"
import time "time"
import flow "flow"
import ui "ui"

module exposes Standup


activity Standup(moves: [11]params.Move, atPosition: bool, sensors: flow.Sensing)
                (motion: ui.MotionStatus, x_setpoint: float64, varyingLimit: params.Limit)
    await true
end
